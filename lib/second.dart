import 'package:flutter/material.dart';

class PolitikPage extends StatelessWidget {
  const PolitikPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ini Halaman kedua")),
    );
  }
}